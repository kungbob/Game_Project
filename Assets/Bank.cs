using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bank : MonoBehaviour {

	// Use this for initialization
	void Start () {
        PlayerStats.IncreaseRate += 1f;
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
